api:
  hostname: http://api.openweathermap.org

management:
         security:
           enabled: false

spring:
  cache:
    type: ehcache
    ehcache:
        config: classpath:ehcache.xml
logging:
  level:
    com.shilton.weather.weatherapp.cache.ehcache: INFO